import React from 'react'

import { Button } from 'typebut'
import 'typebut/dist/index.css'

const App = () => {
  return (
    <>
    <div>

    </div>
      <Button type="primary" text="Primary But" />
      <Button type="default" text="Default But" />
      <Button type="dashed" text="Dashed But" />
      <Button type="text" text="Text But" />
      <Button type="link" text="Link But" />
    </>
  )
}

export default App